export type User = {
    id?: string;
    first_name: string;
    last_name: string;
    password?: string;
    password_hash?: string;
    phone?: number;
    email: string;
    location_id?: string;
  };
  